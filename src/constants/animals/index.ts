import { CatAssets } from './cat'
import { ChickenAssets } from './chicken'
import { DogAssets } from './dog'
import { PigAssets } from './pig'

export const AnimalAssets = {
	dog: DogAssets,
	cat: CatAssets,
	chicken: ChickenAssets,
	pig: PigAssets,
}
